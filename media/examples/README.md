# Examples

Reference media and historical examples used by docs, tests, or operator review.

These files are kept because they document working creative/output patterns. New
throwaway screenshots, Playwright snapshots, local exports, and generated output
should stay untracked. If an example binary must be versioned, classify it in
`../MANIFEST.md` first.
