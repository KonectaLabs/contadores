# Contadores frontend concept ledger

## Concept

Contadores is an operations console, not a marketing site. The UI should feel
quiet, dense, and repeatable: one shell, four operations, clear ownership of
the next action, and no visible implementation state unless it helps an
operator decide what to do next.

Primary operations:

- CRM: lead queue, conversation progress, manual attention, next action, and conversion controls.
- Ads: owned campaign forms, converted-client linking, public URLs, submissions, and Meta event toggles.
- Build: Workstation client work, media, notes, generated artifacts, and Codex.
- Deliver: client lead delivery, source health, recipient context, and retries.

State model:

- `pipeline_stage` is the commercial milestone.
- `queue_state` is who owns the next action.
- `terminal_state` is closed or archived overlay.
- `attention_state` is the urgency signal.
- Legacy `stage` remains a compatibility fallback for backend, bot, script, and
  external-tool contracts; current UI decisions live in the split model plus
  durable conversion evidence.

## Research ledger

- Shopify Polaris empty states
  (https://polaris-react.shopify.com/components/layout-and-structure/empty-state):
  empty views should orient the user, use simple language, and point at one
  primary action.
- Shopify Polaris common actions
  (https://polaris-react.shopify.com/patterns/common-actions): add/edit/copy/delete
  actions should stay near the object they affect, avoid button clutter, and put
  destructive actions at the bottom of action lists.
- Salesforce Lightning data tables
  (https://developer.salesforce.com/docs/component-library/bundle/lightning-datatable/overview):
  record lists can use structured tables, but invalid/uncertain state needs to
  be explicit instead of silently rendering zero-value rows.
- Atlassian foundations (https://atlassian.design/foundations/): shared
  foundations should define tokens, primitives, and panel structure while domain
  surfaces keep contextual freedom.

## Coverage ledger

| Surface | Changed | Left alone intentionally | Verified | Remaining risk |
| --- | --- | --- | --- | --- |
| App shell/navigation | The shell now has a first-class `Ads` operation between CRM and Build so owned campaign form creation is not hidden under Delivery. The old operations-monitoring frontend section was removed completely from navigation, render branches, styles, and default refresh paths. Mobile nav overflow fixed in first state slice; repeated topbar commands now use compact visible labels on desktop (`Funnel`, `Runtime`, `Refresh`) and collapse back to labeled icon buttons only on narrow mobile. | Existing section names in code remain stable for CRM, Build, and Delivery; `campaigns` is the section id for the Ads workspace. | `npm run build`; Browser desktop Ads smoke with visible `Campaign` creation command; Browser 390px Ads creation panel with no horizontal overflow and no console errors. | Needs deployed verification with populated campaign/submission data after first real production campaign is created. |
| CRM states | Replaced visible `Booked` stage with `Converted`; filters now read from conceptual state query params; manual conversion calls `mark-converted`; backend now has canonical `/conversions/mark`, `converted=true`, `converted_at`, and canonical meeting/conversion metric aliases; CRM outbound is now blocked at enqueue time for converted/closed/archived leads while Workstation delivery remains allowed; Calendly webhook now records `meeting_scheduled_at` as a Meeting milestone instead of converting the lead; legacy `needs_human` filter now maps to a visible `Operator` queue view; send/manual copy now speaks operator/follow-up language instead of legacy/manual scheduling language; frontend runtime branching no longer treats `stage=booked` as a live UI state and conversion decisions use split lifecycle fields plus durable conversion evidence; lead filters now render in conceptual groups: Overview, Progress, Action, and System; CRM navigation now normalizes the selected lead, selected bulk rows, active tab, and mode-specific default filter instead of carrying stale detail context between modes; paused banners now distinguish meeting handoffs, meeting scheduled pauses, manual pauses, and operator waits instead of saying everything was paused by an operator. | `raw_stage`, `booked_at`, `mark-booked`, `send-manual-booked`, `send-manual-ping`, and manual attention API names stay for storage/API compatibility; the frontend no longer exposes or filters by `stage=booked` as an operating state. | `python -m compileall -q src/backend/database.py src/backend/endpoints/contadores.py`; `uv run pytest src/backend/tests/test_contadores.py -q`; `npm run build`; Browser desktop/mobile; Browser grouped-filter interaction (`Paused` to `0/2`, `All` back to `2/2`). | Some legacy scripts still have historical stage names, but active requeue paths now check milestone timestamps too. |
| Agent conversion tools | Codex agent toolbelt now exposes `mark_converted`; agent-facing stage editors reject legacy `booked` so conversions go through the canonical action. Canonical conversions and legacy booking aliases now set conversion time without rewriting raw `stage` to `booked`. | Legacy API aliases and DB enum remain for existing callers and historical rows. Agent tool responses still expose the raw `stage` alongside `pipeline_stage`. | `python -m compileall -q src/backend/ai/codex_agent_tools.py src/backend/tests/test_contadores.py`; `uv run pytest src/backend/tests/test_contadores.py -q`; focused canonical conversion tests. | Other older scripts may still mention booked in documentation or historical strategy stats. |
| CRM view selector and chat | Replaced the button-like `Views`/`Filters` selector pattern with one persistent horizontal state strip in the queue bar: every state is a direct count button, the active label stays visible, and the strip scrolls horizontally only when the viewport is too narrow. Chat now uses a neutral timeline panel, a dark teal operator lane, a warm lead lane, stronger role badges, and side rails instead of the previous same-looking green/cream message blocks. Funnel switches still clear strategy/tag filters so hidden secondary filters cannot keep filtering a new funnel. | Backend query params, stored filter values, strategy/tag filtering behavior, message data, tabs, and send flows remain unchanged. Metrics still reflect the current source list before the selected state filter, so counts remain overview signals rather than filtered-subset counts. The old `contadores.dashboard.stageFilter` localStorage key stays as a read-only fallback. | `npm run build`; Browser local DOM check confirms 10 direct state buttons, no visible `Views`/`Filters` control text, horizontal strip overflow instead of a dropdown, and no console errors. Local test data had no populated conversation, so chat verification relied on CSS/build rather than a live message-heavy local thread. | Needs deployed verification against production conversations with inbound/outbound messages after rollout. |
| Lead list/detail | Lead rows and detail header now show conceptual labels; lead cards default to one status signal plus context text instead of stacked strategy/tag/converted chips; detail header now exposes one primary action (`Send` or `Build`) plus `More`, moving `Convert` into the menu. More menu uses `Mark converted` and the conceptual quick-action route. Converted lead details now block `Send` and show `Build` instead of asking the operator to convert an already-converted lead. Empty lead queues now show an actionable empty state with `Clear filters` when filters/search hide all rows. General inbox handoff now reads as `Route to campaign` with a `Handoff point`, while the backend keeps the legacy `stage` payload as compatibility. | Strategy stats keep `booked_rate` field name because API/storage still use it. The inbox routing endpoint still accepts `stage` because old tools and tests use that contract. | `npm run build`; `uv run python -m compileall -q src/backend/database.py src/backend/endpoints/contadores.py`; `uv run pytest src/backend/tests/test_contadores.py -q`; Browser 495px lead-card check confirms zero legacy chip nodes, one visible primary action plus `More`, and no console errors. | Local General inbox had no visible lead, so the mounted route-panel visual still needs a populated-data check. |
| CRM detail action menu | The `More` menu now groups commands by intent (`Client`, `Automation`, `Utilities`, `Danger`) instead of one flat list. The ambiguous `Convert` action is now `Start build`, state-only conversion is `Mark as converted`, `Operator` is `Operator review`, `Copy` is `Copy context`, and primary Build buttons now distinguish `Open workspace` from `Create workspace`. Already-converted leads no longer render a disabled `Mark as converted` command. | The UI handlers still use the existing conversion endpoints and quick actions, now with backend aliases that leave raw `stage` unchanged. | `npm run build`; `git diff --check`; Browser desktop check on unconverted detail (`Send`, grouped menu, `Mark as converted`) and converted detail (`Create workspace`, no disabled `Mark as converted`, no old `Convert`/`Copy`/`Operator` labels); Browser 390px menu check with no horizontal overflow; no console errors. | Deeper action hierarchy and Workstation-specific CTAs can still be refined, but the old `Booked` raw-stage write path is no longer a blocker. |
| Backend lifecycle state | Added `pipeline_stage`, `queue_state`, `terminal_state`, `attention_state`, `conversion_type`, `meeting_scheduled_at`, metrics, and filters. The lifecycle projection fields are now persisted on `contadores_leads`, refreshed by flow updates, Workstation ownership changes, and message activity, with schema backfill for existing rows. Read-side list metrics, filtering, and summaries now derive the conceptual lifecycle from source milestones instead of trusting possibly stale persisted projection values. Queue-time outbound eligibility now shares the same converted/closed/archived lifecycle decision used by pending delivery. Stored conversion evidence now goes through one canonical `lead_is_converted` predicate in lifecycle projections and outbound gates, while public `stage` still respects operator handoff precedence. Conversion writes still leave raw `stage` unchanged and use `booked_at` only as legacy timestamp/evidence, including `/bookings/mark`, `mark-booked`, and `send-manual-booked`. Lead summary and follow-up snapshot responses expose converted leads as `stage=converted`; `raw_stage` keeps the stored legacy enum. Followup snapshots also expose canonical `converted_at` in JSON/CSV and mark `booked`, `booked_at`, and `booked_rate` as legacy aliases for new clients. The legacy `stage=booked` read filter and `booked` metric now behave as aliases for conceptual converted counts; contradictory `converted`/`booked` params and `stage=booked` combinations with `converted=false` or another `pipeline_stage` are rejected so legacy aliases cannot silently disagree with canonical state. Internal follow-up updates and solo-page Workstation starts now record conversions through `mark_converted` without forcing raw `stage=booked`. | No DB enum rename; `raw_stage`, `booked_at`, `mark-booked`, and strategy `booked_rate` names stay as compatibility/read contracts for old callers and historical rows. | `python -m compileall -q src/backend/endpoints/contadores.py src/backend/tests/test_contadores.py`; focused lifecycle tests; `uv run pytest src/backend/tests/test_contadores.py -q`; `npm run build` from `src/frontend`. | Followup/automation still has source-specific exclusions like Venezuela and opt-out; `stage=booked` remains accepted only as a legacy read alias. |
| Setup/config surfaces | Setup now shows a blocker overview first and moves checklist/grid/copy/path into disclosure. Funnel editor keeps identity, enabled, sheet, primary offer, and meeting copy visible while pricing/templates/routing/weights stay behind `Funnel details`. Runtime config keeps only `Enabled` visible and moves meeting URL, alert emails, and strategy weights behind `Advanced controls`. | Config payloads and saved field names stay unchanged. Hidden advanced fields remain editable from the same drawer. | `npm run build`; Browser desktop CRM/Edit funnel/Runtime; Browser 390px drawer disclosure with no horizontal overflow; no console errors. | Setup banner copy still uses compact joined issue text; broader legacy CSS cleanup remains separate. |
| Send/bulk send modals | Closed-state guards now read `terminal_state` where available. Destructive confirmations now use the shared modal language instead of browser-native `window.confirm`, with shared focus/keyboard behavior. Bulk actions now count and execute only on selected leads in the current visible list, with explicit guard messages for stale/no-target submit paths and a visible disabled treatment for blocked send options. | Send option hierarchy still uses the existing modal structure and transport action names. | `npm run build`; Browser desktop and 390px mobile confirm modal, focus trap, Escape close, no console errors. | Send option hierarchy and modal empty states still need a deeper pass with real blocked conversations. |
| Build | Client list now uses human Workstation states; client detail header shows identity, offer, media, and one primary action; Codex/notes/chat/copy/download/close moved into More; run internals moved behind closed Run details; stale/failed run alerts stay visible. Header actions no longer duplicate Start/Steer Codex or Notes inside More when that action is already the primary visible command. More now groups commands into Build controls, Workstation actions, and Client utilities so the menu scans by intent instead of as one flat command list. List loading is now separate from detail loading, so search/refresh does not make Build report the wrong empty/detail state. Build More now uses the shared `ct-action-menu` disclosure instead of a bespoke popover toggle. Switching clients now clears stale detail/notes immediately, ignores late detail responses from older client requests, and shows an explicit detail loading state while the new client workspace fetch is in flight. | Media, photo, notes, and chat panels keep their current content structure. | `npm run build`, full backend tests, Browser desktop Build empty-state smoke with no console errors; Browser local Build smoke after detail-loading guard confirmed no horizontal overflow and no console warnings/errors. | Generated artifact review, notes panel weight, Workstation modals, and populated-client menu screenshots still need a deeper pass. |
| Deliver | Sheet rows now render as operational lead cards with mapped identity, delivery status, notification preview, and collapsed source details. Retryable rows now surface in a compact Next actions section; full rows stay behind disclosure by default and healthy rows no longer show disabled Retry noise. Source/contact configuration moved out of the daily view into a drawer, and `Edit source` is disabled when no contact is selected. The source drawer now preflights required recipient/sheet fields and JSON mappings inline before calling the API, keeping save failures contextual instead of global. Lead cards no longer show raw-field counts, created date, attempt count, or a repeated contact-summary grid in the default card body. Row actions now keep one primary action visible (`Chat` when a WhatsApp link exists, otherwise `Copy`) and move `Copy all` plus row-level retry into `More`; the dedicated Next actions section still keeps `Retry` as the primary command. | Recipient chat structure unchanged. Delivery source API payload and backend validation contract stay the same. | `npm run build`; Browser desktop Delivery drawer; Browser 390px mobile drawer with visible create action and no console errors; local Browser preflight check confirms disabled save with inline errors, then enabled save after minimal valid fields; server deploy healthy; local Browser Delivery smoke after row-action cleanup mounted with no horizontal overflow and no console errors, but local data had no sheet lead rows. | Still needs recipient context with real lead rows on server data and a deeper recipient chat/history pass. |
| Reusable component language | State pills, action menu popovers, scoped tokens, mobile popovers, loading empties, technical disclosures, confirm modals, and Delivery source drawer sections improved; broad unscoped legacy dashboard CSS was removed so current `ct-*` components own the visual system. | Only scoped Contadores styles remain; old compatibility names stay in API/data where needed. | `git diff --check`, build, Browser desktop/mobile confirm checks. | Repeated status/button patterns still need cleanup. |
