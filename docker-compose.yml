services:
  backend:
    build:
      context: .
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app/src
    ports:
      - "8000:8000"
    command: uv run uvicorn backend.main:app --host 0.0.0.0 --port 8000
