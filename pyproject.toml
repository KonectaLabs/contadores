[project]
name = "contadores"
version = "0.1.0"
description = "Scripts para leer datos desde Google Sheets"
requires-python = ">=3.11"
dependencies = [
    "google-api-python-client>=2.0.0",
    "google-auth>=2.0.0",
    "python-dotenv>=1.1.0",
]

[tool.uv.workspace]
members = ["backend"]
